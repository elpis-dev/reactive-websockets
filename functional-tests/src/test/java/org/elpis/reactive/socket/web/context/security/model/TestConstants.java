package org.elpis.reactive.socket.web.context.security.model;

import java.util.UUID;

public interface TestConstants {
    String TEST_VALUE = UUID.randomUUID().toString();

    String PRINCIPAL = "principal";
}
