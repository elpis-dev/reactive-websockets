arch: amd64

language: java

jdk: 
  - openjdk11

cache:
  directories:
  - $HOME/.m2

script:
  - java --version
  - mvn -f /reactive-websockets/pom.xml clean install
  - mvn -f /reactive-websockets-test/pom.xml clean install -DskipTests=false